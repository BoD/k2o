# Next version (unreleased)

# v1.3.0 - 2026-06-19

- Add all missing primitives and parameters.
- `$fa` and `$fs` are now no longer set by default.
- Add a `renderOpenScad()` function to render the DSL to a string.
- Add the `macosArm64` target.

# v1.2.1 - 2026-01-10

Add missing parameters to `linearExtrude()` and `offset()`.

# v1.2.0 - 2026-01-10

Add `minkowski()`.

# v1.1.0 - 2025-12-24

Output to stdout by default.

# v1.0.0 - 2025-12-23

Initial release 🎉
